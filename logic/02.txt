P ∨ (Q ∧ R) ≡ (P ∨ Q) ∧ (P ∨ R)

True, use distributive.

(Q ^ R) doesn't matter on the left because if P is true, 
both parts on the right are true so the  ∧  is true.

If P is false, then on the left Q and R both must be true 
in order for P ∨ (Q ∧ R) to be true, so on the right its the 
same.
