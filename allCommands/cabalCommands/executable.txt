$ cd project_dir           -- change filename to Main.hs (copy)

$ cabal init

$ cabal configure   -- optional
$ cabal build       -- optional

$ cabal install



on OSX:
$ cabal xProjectName.cabal  -- executable is in the .cabal file

on Linux:
$ cabal run xProjectName.cabal
